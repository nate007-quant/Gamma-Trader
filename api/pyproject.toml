[project]
name = "gamma-trader-api"
version = "0.1.0"
description = "Local API for Gamma Trader dashboard"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
  "pyyaml>=6.0",
  "pandas>=2.2",
]

[tool.ruff]
line-length = 100
